#include <stdio.h>

int main(){
    //Operator Precedence 
    //Operators of Higher Priority are Evaluated 1st in Absence of Parenthesis
    //*/% > +- > =

    //Operator Associativity
    //When Operators of Same Priority are used the result is evaluated from left to right this called Operator Associativity

    //Always first see the Precedence then go for Associativity

    //Pro Tip: Always Use Parenthesis 
    return 0;
}