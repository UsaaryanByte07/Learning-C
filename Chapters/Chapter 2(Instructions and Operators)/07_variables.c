#include <stdio.h>

int main(){
    int i = 10;
    int j = i;
    //%d is called the format Specifier
    //%d is for int,%f is for float,%c is for char
    printf("The value of i is %d\n",i);
    printf("The value of j is %d\n",j);
    
    int a=12,b=23,c=22,d=34; //Declaring Multiple Variables and initializing them

    return 0;
}