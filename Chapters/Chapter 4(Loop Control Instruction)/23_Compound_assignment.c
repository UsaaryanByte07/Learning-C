#include <stdio.h>

int main(){
    int b =1;
    b += 2; //Same as b = b + 2
    b -= 3; //Same as b = b - 3
    b *= 4; //Same as b = b * 4
    b /= 9; //Same as b = b / 9
    b %= 2; //Same as b = b % 2
    return 0;
}