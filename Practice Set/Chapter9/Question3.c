#include <stdio.h>

int main(){
    /* 3. Twenty integers are to be stored in memory. What will you prefer- Array or structure? */

    /* Array is a better Option to use here because the datatypes of the variable to be stored is same. */
    return 0;
}